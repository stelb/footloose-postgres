---
postgres_version:
  - '9.6'
postgres_version_dotless: "{{ postgres_version | replace('.', '') }}"
postgres_primary: "{{play_hosts[0]}}"

postgres_cluster:

centos_repo: https://download.postgresql.org/pub/repos/yum/reporpms/EL-7-x86_64/pgdg-redhat-repo-latest.noarch.rpm
debian_repo: 'deb http://apt.postgresql.org/pub/repos/apt/ buster-pgdg main'
debian_repo_key: https://www.postgresql.org/media/keys/ACCC4CF8.asc
